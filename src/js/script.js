document.addEventListener("DOMContentLoaded", () => {
    // Variabel untuk container popular car dan available car

    // variabel untuk sorting

    // fungsi untuk render popular car ke dalam container
    function renderPopularCar() {

        // render popular car ke dalam container

    }

    // fungsi untuk render available car ke dalam container
    function renderAvailableCar() {
        // ini untuk variiabel sorting dulu sebelum di render
        

        // render ke dalam container

    }

    renderAvailableCar();
    renderPopularCar();

    // event listener untuk tombol sort
    
});
